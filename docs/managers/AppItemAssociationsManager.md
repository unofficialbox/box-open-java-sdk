<!-- Generated by box-gantry. DO NOT EDIT. -->
# AppItemAssociationsManager

Reach these methods through the `appItemAssociations` field on `dev.unofficialbox.Client`.
Each is a **blocking** call returning its value directly and throwing
`dev.unofficialbox.runtime.Runtime.BoxApiException` on failure.

## listFile

`GET /files/{file_id}/app_item_associations`

| Parameter | In | Type | Required |
|---|---|---|---|
| `file_id` | path | `String` | yes |
| `limit` | query | `Long` | no |
| `marker` | query | `String` | no |
| `application_type` | query | `String` | no |

**Returns:** `AppItemAssociations`

Paginated — `listFile(...)` returns an auto-paging `Iterable` you
loop with `for (var item : …)`, threading the cursor for you. See
the [pagination guide](../pagination.md).

## listFolder

`GET /folders/{folder_id}/app_item_associations`

| Parameter | In | Type | Required |
|---|---|---|---|
| `folder_id` | path | `String` | yes |
| `limit` | query | `Long` | no |
| `marker` | query | `String` | no |
| `application_type` | query | `String` | no |

**Returns:** `AppItemAssociations`

Paginated — `listFolder(...)` returns an auto-paging `Iterable` you
loop with `for (var item : …)`, threading the cursor for you. See
the [pagination guide](../pagination.md).

