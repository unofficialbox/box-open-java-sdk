<!-- Generated by box-gantry. DO NOT EDIT. -->
# LegalHoldPolicyAssignmentsManager

Reach these methods through the `legalHoldPolicyAssignments` field on `dev.unofficialbox.Client`.
Each is a **blocking** call returning its value directly and throwing
`dev.unofficialbox.runtime.Runtime.BoxApiException` on failure.

## list

`GET /legal_hold_policy_assignments`

| Parameter | In | Type | Required |
|---|---|---|---|
| `policy_id` | query | `String` | yes |
| `assign_to_type` | query | `LegalHoldPolicyAssignedItemType` | no |
| `assign_to_id` | query | `String` | no |
| `marker` | query | `String` | no |
| `limit` | query | `Long` | no |
| `fields` | query | `List<String>` | no |

**Returns:** `LegalHoldPolicyAssignments`

Paginated — `list(...)` returns an auto-paging `Iterable` you
loop with `for (var item : …)`, threading the cursor for you. See
the [pagination guide](../pagination.md).

## create

`POST /legal_hold_policy_assignments`

**Request body** (`application/json`): `LegalHoldPolicyAssignmentCreateRequest`

**Returns:** `LegalHoldPolicyAssignment`

## get

`GET /legal_hold_policy_assignments/{legal_hold_policy_assignment_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `legal_hold_policy_assignment_id` | path | `String` | yes |

**Returns:** `LegalHoldPolicyAssignment`

## delete

`DELETE /legal_hold_policy_assignments/{legal_hold_policy_assignment_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `legal_hold_policy_assignment_id` | path | `String` | yes |

**Returns:** no content (`void`)

## listFileOnHold

`GET /legal_hold_policy_assignments/{legal_hold_policy_assignment_id}/files_on_hold`

| Parameter | In | Type | Required |
|---|---|---|---|
| `legal_hold_policy_assignment_id` | path | `String` | yes |
| `marker` | query | `String` | no |
| `limit` | query | `Long` | no |
| `fields` | query | `List<String>` | no |

**Returns:** `FilesOnHold`

Paginated — `listFileOnHold(...)` returns an auto-paging `Iterable` you
loop with `for (var item : …)`, threading the cursor for you. See
the [pagination guide](../pagination.md).

## listFileVersionOnHold

`GET /legal_hold_policy_assignments/{legal_hold_policy_assignment_id}/file_versions_on_hold`

| Parameter | In | Type | Required |
|---|---|---|---|
| `legal_hold_policy_assignment_id` | path | `String` | yes |
| `marker` | query | `String` | no |
| `limit` | query | `Long` | no |
| `fields` | query | `List<String>` | no |

**Returns:** `FileVersionsOnHold`

Paginated — `listFileVersionOnHold(...)` returns an auto-paging `Iterable` you
loop with `for (var item : …)`, threading the cursor for you. See
the [pagination guide](../pagination.md).

