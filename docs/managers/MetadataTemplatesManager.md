<!-- Generated by box-gantry. DO NOT EDIT. -->
# MetadataTemplatesManager

Reach these methods through the `metadataTemplates` field on `dev.unofficialbox.Client`.
Each is a **blocking** call returning its value directly and throwing
`dev.unofficialbox.runtime.Runtime.BoxApiException` on failure.

## list

`GET /metadata_templates`

| Parameter | In | Type | Required |
|---|---|---|---|
| `metadata_instance_id` | query | `String` | yes |
| `marker` | query | `String` | no |
| `limit` | query | `Long` | no |

**Returns:** `MetadataTemplates`

Paginated — `list(...)` returns an auto-paging `Iterable` you
loop with `for (var item : …)`, threading the cursor for you. See
the [pagination guide](../pagination.md).

## getSchema

`GET /metadata_templates/{scope}/{template_key}/schema`

| Parameter | In | Type | Required |
|---|---|---|---|
| `scope` | path | `GetFileIdMetadataIdIdScope` | yes |
| `template_key` | path | `String` | yes |

**Returns:** `MetadataTemplate`

## updateSchema

`PUT /metadata_templates/{scope}/{template_key}/schema`

| Parameter | In | Type | Required |
|---|---|---|---|
| `scope` | path | `GetFileIdMetadataIdIdScope` | yes |
| `template_key` | path | `String` | yes |

**Request body** (`application/json-patch+json`): `List<SchemaUpdateRequest>`

**Returns:** `MetadataTemplate`

## deleteSchema

`DELETE /metadata_templates/{scope}/{template_key}/schema`

| Parameter | In | Type | Required |
|---|---|---|---|
| `scope` | path | `GetFileIdMetadataIdIdScope` | yes |
| `template_key` | path | `String` | yes |

**Returns:** no content (`void`)

## get

`GET /metadata_templates/{template_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `template_id` | path | `String` | yes |

**Returns:** `MetadataTemplate`

## listGlobal

`GET /metadata_templates/global`

| Parameter | In | Type | Required |
|---|---|---|---|
| `marker` | query | `String` | no |
| `limit` | query | `Long` | no |

**Returns:** `MetadataTemplates`

Paginated — `listGlobal(...)` returns an auto-paging `Iterable` you
loop with `for (var item : …)`, threading the cursor for you. See
the [pagination guide](../pagination.md).

## listEnterprise

`GET /metadata_templates/enterprise`

| Parameter | In | Type | Required |
|---|---|---|---|
| `marker` | query | `String` | no |
| `limit` | query | `Long` | no |

**Returns:** `MetadataTemplates`

Paginated — `listEnterprise(...)` returns an auto-paging `Iterable` you
loop with `for (var item : …)`, threading the cursor for you. See
the [pagination guide](../pagination.md).

## createSchema

`POST /metadata_templates/schema`

**Request body** (`application/json`): `SchemaCreateRequest`

**Returns:** `MetadataTemplate`

