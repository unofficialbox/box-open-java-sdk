<!-- Generated by box-gantry. DO NOT EDIT. -->
# ShieldListsManager

Reach these methods through the `shieldLists` field on `dev.unofficialbox.Client`.
Each is a **blocking** call returning its value directly and throwing
`dev.unofficialbox.runtime.Runtime.BoxApiException` on failure.

## list

`GET /shield_lists`

**Returns:** `ShieldLists`

**Example**

```java
var result = client.shieldLists.list();
```

## create

`POST /shield_lists`

**Request body** (`application/json`): `ShieldListsCreate`

**Returns:** `ShieldList`

**Example**

```java
var result = client.shieldLists.create(new ShieldListsCreate(/* … */));
```

## get

`GET /shield_lists/{shield_list_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `shield_list_id` | path | `String` | yes |

**Returns:** `ShieldList`

**Example**

```java
var result = client.shieldLists.get("SHIELD_LIST_ID");
```

## update

`PUT /shield_lists/{shield_list_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `shield_list_id` | path | `String` | yes |

**Request body** (`application/json`): `ShieldListsUpdate`

**Returns:** `ShieldList`

**Example**

```java
var result = client.shieldLists.update("SHIELD_LIST_ID", new ShieldListsUpdate(/* … */));
```

## delete

`DELETE /shield_lists/{shield_list_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `shield_list_id` | path | `String` | yes |

**Returns:** no content (`void`)

**Example**

```java
client.shieldLists.delete("SHIELD_LIST_ID");
```

