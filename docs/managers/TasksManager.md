<!-- Generated by box-gantry. DO NOT EDIT. -->
# TasksManager

Reach these methods through the `tasks` field on `dev.unofficialbox.Client`.
Each is a **blocking** call returning its value directly and throwing
`dev.unofficialbox.runtime.Runtime.BoxApiException` on failure.

## listFile

`GET /files/{file_id}/tasks`

| Parameter | In | Type | Required |
|---|---|---|---|
| `file_id` | path | `String` | yes |

**Returns:** `Tasks`

**Example**

```java
var result = client.tasks.listFile("FILE_ID");
```

## create

`POST /tasks`

**Request body** (`application/json`): `TaskCreateRequest`

**Returns:** `Task`

**Example**

```java
var result = client.tasks.create(new TaskCreateRequest(/* … */));
```

## get

`GET /tasks/{task_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `task_id` | path | `String` | yes |

**Returns:** `Task`

**Example**

```java
var result = client.tasks.get("TASK_ID");
```

## update

`PUT /tasks/{task_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `task_id` | path | `String` | yes |

**Request body** (`application/json`): `TaskUpdateRequest`

**Returns:** `Task`

**Example**

```java
var result = client.tasks.update("TASK_ID", new TaskUpdateRequest(/* … */));
```

## delete

`DELETE /tasks/{task_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `task_id` | path | `String` | yes |

**Returns:** no content (`void`)

**Example**

```java
client.tasks.delete("TASK_ID");
```

