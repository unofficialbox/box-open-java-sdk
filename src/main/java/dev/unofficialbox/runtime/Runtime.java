// Code generated by gantry-contract — do not edit.
//
// Compilable stubs for the hand-written runtime surface (FR-5.3).
// Generated SDK code compiles against this class; the real runtime
// implements the same contract.
package dev.unofficialbox.runtime;

/** The hand-written runtime surface (FR-5). */
public final class Runtime {
    private Runtime() {}

    /** The runtime-owned HTTP request envelope. */
    public static final class Request {}

    /** The runtime-owned HTTP response envelope. */
    public static final class Response {}

    /** A streaming body (buffered on platforms whose manifest says so). */
    public static final class Stream {
        /** An empty stream (e.g. an absent multipart file part). */
        public static Stream empty() {
            throw new UnsupportedOperationException("gantryruntime stub: Stream.empty is not wired to a real runtime");
        }

        /** A stream over in-memory bytes (an octet-stream request body). */
        public static Stream fromBytes(byte[] data) {
            throw new UnsupportedOperationException("gantryruntime stub: Stream.fromBytes is not wired to a real runtime");
        }
    }

    /** The configured authentication flow (developer token, CCG, OAuth, JWT). */
    public static final class Auth {}

    /** A runtime error: a failed request, auth acquisition, or body decode. */
    public static final class BoxApiException extends RuntimeException {
        private static final long serialVersionUID = 1L;

        public BoxApiException(String message) {
            super(message);
        }
    }

    /**
     * The runtime session: auth, base URLs, HTTP client, retry policy.
     * Session-receiver contract functions are its methods.
     */
    public static final class Session {
        /** Build a runtime session for an authentication flow. */
        public Session(Auth auth) {
            throw new UnsupportedOperationException("gantryruntime stub: Session is not wired to a real runtime");
        }

        /** Execute the request with retries: exponential backoff + jitter, 401 token refresh, Retry-After handling (R§1 network layer). */
        public Response fetch(Request request) {
            throw new UnsupportedOperationException("gantryruntime stub: fetch is not wired to a real runtime");
        }

        /** Return a valid access token for the configured auth flow, acquiring or refreshing through the pluggable token store as needed. */
        public String accessToken() {
            throw new UnsupportedOperationException("gantryruntime stub: access_token is not wired to a real runtime");
        }

        /** The configured base URL for a D-106 class ("api", "api_root", "upload", "upload_session", "oauth_authorize", "download"), without a trailing slash. Runtime configuration may override any of them (custom deployments). */
        public String baseUrl(String name) {
            throw new UnsupportedOperationException("gantryruntime stub: base_url is not wired to a real runtime");
        }

        /** Create a request envelope for the given method and fully built URL. */
        public Request newRequest(String method, String url) {
            throw new UnsupportedOperationException("gantryruntime stub: new_request is not wired to a real runtime");
        }
    }

    /** Return the request with the header set. */
    public static Request withHeader(Request request, String name, String value) {
        throw new UnsupportedOperationException("gantryruntime stub: with_header is not wired to a real runtime");
    }

    /** Return the request with the query parameter appended, encoded. */
    public static Request withQuery(Request request, String name, String value) {
        throw new UnsupportedOperationException("gantryruntime stub: with_query is not wired to a real runtime");
    }

    /** Return the request with the serialized JSON body and content type set. */
    public static Request withJsonBody(Request request, byte[] body) {
        throw new UnsupportedOperationException("gantryruntime stub: with_json_body is not wired to a real runtime");
    }

    /** Return the request with an application/x-www-form-urlencoded body (the OAuth2 token endpoints). */
    public static Request withFormBody(Request request, byte[] form) {
        throw new UnsupportedOperationException("gantryruntime stub: with_form_body is not wired to a real runtime");
    }

    /** Return the request with a streaming body (buffered on platforms whose manifest says so — FR-7.4). */
    public static Request withStreamBody(Request request, Stream body, String contentType) {
        throw new UnsupportedOperationException("gantryruntime stub: with_stream_body is not wired to a real runtime");
    }

    /** Return the request with a Box-style multipart body: an `attributes` JSON part plus a file part (G-7). */
    public static Request withMultipartBody(Request request, byte[] attributes, String fileName, Stream file) {
        throw new UnsupportedOperationException("gantryruntime stub: with_multipart_body is not wired to a real runtime");
    }

    /** Read the whole response body. */
    public static byte[] responseBytes(Response response) {
        throw new UnsupportedOperationException("gantryruntime stub: response_bytes is not wired to a real runtime");
    }

    /** The response body as a stream, for binary downloads (FR-7.4). */
    public static Stream responseStream(Response response) {
        throw new UnsupportedOperationException("gantryruntime stub: response_stream is not wired to a real runtime");
    }

    /** A response header value, empty when absent (redirect Location, Retry-After surfacing). */
    public static String responseHeader(Response response, String name) {
        throw new UnsupportedOperationException("gantryruntime stub: response_header is not wired to a real runtime");
    }

    /** The response status code. */
    public static long statusCode(Response response) {
        throw new UnsupportedOperationException("gantryruntime stub: status_code is not wired to a real runtime");
    }
}
