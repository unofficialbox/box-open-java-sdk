// Code generated by box-gantry 0.1.0 (spec ee7d55aedefe2fa0). DO NOT EDIT.
package dev.unofficialbox.managers;

/**
 * Paginator over {@link MetadataTaxonomiesManager#listNodesPage}, iterating every
 * {@code dev.unofficialbox.model.schemas.MetadataTaxonomyNode} across pages (FR-7.3). Threads the response cursor back
 * into the request so callers can write {@code for (var item : paginator)}.
 * Re-iterable: each {@code iterator()} is an independent pass over a private
 * copy of the options, so the caller's options are never mutated.
 */
public final class MetadataTaxonomiesListNodesPaginator implements java.lang.Iterable<dev.unofficialbox.model.schemas.MetadataTaxonomyNode> {
    private final dev.unofficialbox.runtime.Runtime.Session session;
    private final String namespace;
    private final String taxonomyKey;
    private final MetadataTaxonomiesManager.ListNodesOptions options;

    MetadataTaxonomiesListNodesPaginator(dev.unofficialbox.runtime.Runtime.Session session, String namespace, String taxonomyKey, MetadataTaxonomiesManager.ListNodesOptions options) {
        this.session = session;
        this.namespace = namespace;
        this.taxonomyKey = taxonomyKey;
        this.options = options;
    }

    private MetadataTaxonomiesManager.ListNodesOptions _freshOptions() {
        MetadataTaxonomiesManager.ListNodesOptions _o = new MetadataTaxonomiesManager.ListNodesOptions();
        if (options != null) {
            _o.level = options.level;
            _o.parent = options.parent;
            _o.ancestor = options.ancestor;
            _o.query = options.query;
            _o.includeTotalResultCount = options.includeTotalResultCount;
            _o.marker = options.marker;
            _o.limit = options.limit;
        }
        return _o;
    }

    @Override
    public java.util.Iterator<dev.unofficialbox.model.schemas.MetadataTaxonomyNode> iterator() {
        return new java.util.Iterator<dev.unofficialbox.model.schemas.MetadataTaxonomyNode>() {
            private final MetadataTaxonomiesManager _manager = new MetadataTaxonomiesManager(session);
            private final MetadataTaxonomiesManager.ListNodesOptions _opts = _freshOptions();
            private String _cursor = _opts.marker;
            private java.util.Iterator<dev.unofficialbox.model.schemas.MetadataTaxonomyNode> _buffer = java.util.Collections.emptyIterator();
            private boolean _done = false;

            private void _advance() {
                while (!_buffer.hasNext() && !_done) {
                    _opts.marker = _cursor;
                    dev.unofficialbox.model.schemas.MetadataTaxonomyNodes _page = _manager.listNodesPage(namespace, taxonomyKey, _opts);
                    java.util.List<dev.unofficialbox.model.schemas.MetadataTaxonomyNode> _items = _page.entries().orElse(java.util.List.of());
                    _buffer = _items.iterator();
                    String _next = _page.nextMarker().isPresent() ? _page.nextMarker().value() : null;
                    if (_next != null && !_next.isEmpty()) {
                        _cursor = _next;
                    } else {
                        _done = true;
                    }
                }
            }

            @Override
            public boolean hasNext() {
                _advance();
                return _buffer.hasNext();
            }

            @Override
            public dev.unofficialbox.model.schemas.MetadataTaxonomyNode next() {
                _advance();
                if (!_buffer.hasNext()) {
                    throw new java.util.NoSuchElementException();
                }
                return _buffer.next();
            }
        };
    }
}
