// Code generated by box-gantry 0.1.0 (spec ee7d55aedefe2fa0). DO NOT EDIT.
package dev.unofficialbox.managers;

/**
 * Paginator over {@link ShieldInformationBarriersManager#listPage}, iterating every
 * {@code dev.unofficialbox.model.schemas.ShieldInformationBarrier} across pages (FR-7.3). Threads the response cursor back
 * into the request so callers can write {@code for (var item : paginator)}.
 * Re-iterable: each {@code iterator()} is an independent pass over a private
 * copy of the options, so the caller's options are never mutated.
 */
public final class ShieldInformationBarriersListPaginator implements java.lang.Iterable<dev.unofficialbox.model.schemas.ShieldInformationBarrier> {
    private final dev.unofficialbox.runtime.Runtime.Session session;
    private final ShieldInformationBarriersManager.ListOptions options;

    ShieldInformationBarriersListPaginator(dev.unofficialbox.runtime.Runtime.Session session, ShieldInformationBarriersManager.ListOptions options) {
        this.session = session;
        this.options = options;
    }

    private ShieldInformationBarriersManager.ListOptions _freshOptions() {
        ShieldInformationBarriersManager.ListOptions _o = new ShieldInformationBarriersManager.ListOptions();
        if (options != null) {
            _o.marker = options.marker;
            _o.limit = options.limit;
        }
        return _o;
    }

    @Override
    public java.util.Iterator<dev.unofficialbox.model.schemas.ShieldInformationBarrier> iterator() {
        return new java.util.Iterator<dev.unofficialbox.model.schemas.ShieldInformationBarrier>() {
            private final ShieldInformationBarriersManager _manager = new ShieldInformationBarriersManager(session);
            private final ShieldInformationBarriersManager.ListOptions _opts = _freshOptions();
            private String _cursor = _opts.marker;
            private java.util.Iterator<dev.unofficialbox.model.schemas.ShieldInformationBarrier> _buffer = java.util.Collections.emptyIterator();
            private boolean _done = false;

            private void _advance() {
                while (!_buffer.hasNext() && !_done) {
                    _opts.marker = _cursor;
                    dev.unofficialbox.model.schemas.ShieldInformationBarriers _page = _manager.listPage(_opts);
                    java.util.List<dev.unofficialbox.model.schemas.ShieldInformationBarrier> _items = _page.entries().orElse(java.util.List.of());
                    _buffer = _items.iterator();
                    String _next = _page.nextMarker().isPresent() ? _page.nextMarker().value() : null;
                    if (_next != null && !_next.isEmpty()) {
                        _cursor = _next;
                    } else {
                        _done = true;
                    }
                }
            }

            @Override
            public boolean hasNext() {
                _advance();
                return _buffer.hasNext();
            }

            @Override
            public dev.unofficialbox.model.schemas.ShieldInformationBarrier next() {
                _advance();
                if (!_buffer.hasNext()) {
                    throw new java.util.NoSuchElementException();
                }
                return _buffer.next();
            }
        };
    }
}
