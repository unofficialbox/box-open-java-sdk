// Code generated by box-gantry 0.1.0 (spec ee7d55aedefe2fa0). DO NOT EDIT.
package dev.unofficialbox.managers;

/**
 * Paginator over {@link AiStudioManager#listAiAgentsPage}, iterating every
 * {@code dev.unofficialbox.model.schemas.AiSingleAgentResponseFull} across pages (FR-7.3). Threads the response cursor back
 * into the request so callers can write {@code for (var item : paginator)}.
 * Re-iterable: each {@code iterator()} is an independent pass over a private
 * copy of the options, so the caller's options are never mutated.
 */
public final class AiStudioListAiAgentsPaginator implements java.lang.Iterable<dev.unofficialbox.model.schemas.AiSingleAgentResponseFull> {
    private final dev.unofficialbox.runtime.Runtime.Session session;
    private final AiStudioManager.ListAiAgentsOptions options;

    AiStudioListAiAgentsPaginator(dev.unofficialbox.runtime.Runtime.Session session, AiStudioManager.ListAiAgentsOptions options) {
        this.session = session;
        this.options = options;
    }

    private AiStudioManager.ListAiAgentsOptions _freshOptions() {
        AiStudioManager.ListAiAgentsOptions _o = new AiStudioManager.ListAiAgentsOptions();
        if (options != null) {
            _o.mode = options.mode;
            _o.fields = options.fields;
            _o.agentState = options.agentState;
            _o.includeBoxDefault = options.includeBoxDefault;
            _o.marker = options.marker;
            _o.limit = options.limit;
        }
        return _o;
    }

    @Override
    public java.util.Iterator<dev.unofficialbox.model.schemas.AiSingleAgentResponseFull> iterator() {
        return new java.util.Iterator<dev.unofficialbox.model.schemas.AiSingleAgentResponseFull>() {
            private final AiStudioManager _manager = new AiStudioManager(session);
            private final AiStudioManager.ListAiAgentsOptions _opts = _freshOptions();
            private String _cursor = _opts.marker;
            private java.util.Iterator<dev.unofficialbox.model.schemas.AiSingleAgentResponseFull> _buffer = java.util.Collections.emptyIterator();
            private boolean _done = false;

            private void _advance() {
                while (!_buffer.hasNext() && !_done) {
                    _opts.marker = _cursor;
                    dev.unofficialbox.model.schemas.AiMultipleAgentResponse _page = _manager.listAiAgentsPage(_opts);
                    java.util.List<dev.unofficialbox.model.schemas.AiSingleAgentResponseFull> _items = _page.entries();
                    _buffer = _items.iterator();
                    String _next = _page.nextMarker().isPresent() ? _page.nextMarker().value() : null;
                    if (_next != null && !_next.isEmpty()) {
                        _cursor = _next;
                    } else {
                        _done = true;
                    }
                }
            }

            @Override
            public boolean hasNext() {
                _advance();
                return _buffer.hasNext();
            }

            @Override
            public dev.unofficialbox.model.schemas.AiSingleAgentResponseFull next() {
                _advance();
                if (!_buffer.hasNext()) {
                    throw new java.util.NoSuchElementException();
                }
                return _buffer.next();
            }
        };
    }
}
